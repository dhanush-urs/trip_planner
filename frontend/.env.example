# TripForge Frontend — Environment Variables
# Copy to .env before running locally:
#   cp .env.example .env

# API Gateway base URL (all backend calls go through here)
VITE_API_BASE_URL=http://localhost:8080
