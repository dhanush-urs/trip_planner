package com.tripforge.auth.config;

import org.springframework.context.annotation.Configuration;

/**
 * General application configuration placeholder.
 * Bean definitions for PasswordEncoder and AuthenticationManager
 * are in SecurityConfig to avoid circular dependency issues.
 */
@Configuration
public class AppConfig {
    // Additional beans can be added here as the service grows
}
